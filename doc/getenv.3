NAME
    getenv(3)     	- get environment entry

SYNOPSIS
    char *getenv( name )
      char *name;

DESCRIPTION
    If the entry "name=value" exists in the user environment, then
    a pointer to "value" is returned. Otherwise NULL is returned.

SEE ALSO
    execve(2), putenv(3), environ(4)
